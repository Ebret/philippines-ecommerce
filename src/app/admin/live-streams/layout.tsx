export const metadata = {
  title: 'Live Streams Management | Admin Dashboard',
  description: 'Manage and monitor all live selling sessions',
};

export default function AdminLiveStreamsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

