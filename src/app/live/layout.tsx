export const metadata = {
  title: 'Live Selling | Philippines E-Commerce',
  description: 'Watch live streams and shop in real-time',
};

export default function LiveLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

