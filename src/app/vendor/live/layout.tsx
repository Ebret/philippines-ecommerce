export const metadata = {
  title: 'Live Selling Dashboard | Vendor',
  description: 'Manage your live streaming sessions',
};

export default function VendorLiveLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

